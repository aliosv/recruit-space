block('section').content()(function() {
    return {
        elem : 'width',
        mix : { block : 'page', elem : 'width' },
        content : {
            elem : 'inner',
            content : this.ctx.content
        }
    };
});
